# Database Configuration
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=documents
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
# JOBS_TABLE=jobs  # Optional: Override the default jobs table name

# Qdrant Configuration
QDRANT_HOST=localhost
QDRANT_PORT=6333
QDRANT_COLLECTION=documents
# QDRANT_URL=http://localhost:6333  # Optional: Override the constructed URL

# LLM Model Configuration
LLM_MODEL_NAME=Mistral-7B-Instruct-v0.3-Q8_0.gguf
LLM_N_CTX=32768
LLM_N_THREADS=4
LLM_N_BATCH=64

# Embedding Model Configuration
EMBEDDING_MODEL_NAME=BAAI/bge-small-en-v1.5

# RAG Configuration
RAG_DEFAULT_LIMIT=5
RAG_MAX_TOKENS=1000

# File Storage Configuration
DOCUMENTS_STORAGE_DIR=/app/documents_storage
