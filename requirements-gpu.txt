# llama-cpp-python is intentionally NOT here: its PyPI (CPU) and cu126 (CUDA)
# wheels share a version, so an --extra-index-url lets pip pick the CPU one. The
# build installs it separately with the CUDA index as primary (see build-release).
--extra-index-url https://download.pytorch.org/whl/cu126
torch==2.12.1
cupy-cuda12x==14.1.1
