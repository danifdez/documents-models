List every date or time expression in the text. Copy each expression verbatim and in order. Include absolute dates, years, date ranges, month/year values and relative expressions. Exclude durations, ages, page numbers, phone numbers, money and version numbers. Return only a JSON array of strings.

Text:
{text}
