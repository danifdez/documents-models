For every date expression below, return a short title of the event it describes. Copy each expression verbatim and return only a JSON array of objects with `expression` and `title` fields. Use an empty title when the surrounding text gives no event.

Text:
{text}

Expressions:
{expressions}
