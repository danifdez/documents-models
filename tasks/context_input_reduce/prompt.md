Merge these ordered digests from one larger current user message into a single faithful digest. Preserve all explicit requests, constraints, questions, named entities, concrete data, examples, ordering cues, distinctions between user requests and quoted content, and stated uncertainty. Do not answer or execute the request. Resolve repetition without inventing facts. Return only the merged digest.
