Given these entities and text, extract relationships between the entities.

Entities:
{entities}

Text:
{text}

Return a JSON array. Each element: {{"subject": "entity name", "predicate": "relationship", "object": "entity name"}}
Use entity names exactly as listed. Predicate examples: works_for, located_in, leads, member_of, part_of, allied_with, founded, governs.

JSON:
