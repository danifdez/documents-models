# Detect language

Language detection identifies the language of one or more text samples.

## Result

Each sample receives a two-letter language code such as `en` for English, `es` for Spanish, or `fr` for French. Results preserve the same order as the supplied samples and include the original text for reference.

Each sample is analyzed independently. If detection cannot be completed, Documents reports the action as failed instead of assigning a guessed default language.
